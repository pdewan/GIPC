/**
 * Implements the non-distributed aspects of the application, creating the 
 * interactive user-interface using the MVC design pattern.
 * Look in order at the model (data), view (output) and controller (input) classes.
 */
/**
 * @author Dewan
 *
 */
package examples.nio.manager.mvc;