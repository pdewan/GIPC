package port.old;

//this seems like a redundant interface
public interface OldInputPort  extends Connectable{
	//should this be only in the group communication. Probably. In which case, we do not need InputPort
	//String[] getPeerNames();


}
